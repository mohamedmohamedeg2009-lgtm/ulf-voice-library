"use client";
import { useState } from "react"; import { Pencil,Plus,Trash2 } from "lucide-react"; import { Button } from "@/components/ui/button"; import { Card,CardContent } from "@/components/ui/card"; import type { Voice } from "./voice";
export function VoiceAdmin({initialVoices}:{initialVoices:Voice[]}){const[voices,setVoices]=useState(initialVoices);const[open,setOpen]=useState(false);const[name,setName]=useState("");function add(e:React.FormEvent){e.preventDefault();const now=new Date().toISOString();setVoices(v=>[{...v[0],id:crypto.randomUUID(),name:`custom-${Date.now()}`,displayName:name,providerVoiceId:`pending-${Date.now()}`,isActive:false,createdAt:now,updatedAt:now},...v]);setName("");setOpen(false)}return <>{open?<Card className="mb-5"><CardContent><form onSubmit={add} className="flex flex-col gap-3 sm:flex-row sm:items-end"><label className="flex-1 text-sm text-slate-300">اسم العرض<input value={name} onChange={e=>setName(e.target.value)} required className="mt-2 min-h-11 w-full rounded-xl border border-white/10 bg-[#091322] px-3"/></label><Button type="submit">إضافة كمسودة</Button><Button type="button" variant="ghost" onClick={()=>setOpen(false)}>إلغاء</Button></form></CardContent></Card>:<Button className="mb-5" onClick={()=>setOpen(true)}><Plus className="size-4"/>إضافة صوت</Button>}<Card><CardContent className="divide-y divide-white/6 p-0">{voices.map(v=><div key={v.id} className="flex items-center gap-3 p-4"><span className="grid size-10 place-items-center rounded-xl bg-cyan-300/8 text-cyan-200">{v.displayName[0]}</span><div className="min-w-0 flex-1"><b className="text-sm text-white">{v.displayName}</b><p className="truncate text-xs text-slate-500">{v.provider} · {v.providerVoiceId}</p></div><span className={`rounded-full px-2 py-1 text-[10px] ${v.isActive?"bg-emerald-400/10 text-emerald-300":"bg-amber-400/10 text-amber-300"}`}>{v.isActive?"نشط":"مسودة"}</span><Button size="icon" variant="ghost" aria-label={`تعديل ${v.displayName}`}><Pencil className="size-4"/></Button><Button size="icon" variant="danger" aria-label={`حذف ${v.displayName}`} onClick={()=>setVoices(current=>current.filter(item=>item.id!==v.id))}><Trash2 className="size-4"/></Button></div>)}</CardContent></Card></>}
