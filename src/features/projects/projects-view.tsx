"use client";
import { useState } from "react"; import { FolderKanban,Plus,Trash2 } from "lucide-react"; import { Button } from "@/components/ui/button"; import { Card,CardContent } from "@/components/ui/card"; import { useAppStore } from "@/store/app-store";
export function ProjectsView(){const[name,setName]=useState("");const[description,setDescription]=useState("");const[open,setOpen]=useState(false);const projects=useAppStore(s=>s.projects);const create=useAppStore(s=>s.createProject);const remove=useAppStore(s=>s.deleteProject);function submit(e:React.FormEvent){e.preventDefault();if(create({name,description})){setName("");setDescription("");setOpen(false)}}return <>{open?<Card className="mb-5"><CardContent><form onSubmit={submit} className="grid gap-3"><label className="text-sm text-slate-300">اسم المشروع<input value={name} onChange={e=>setName(e.target.value)} maxLength={100} required className="mt-2 min-h-11 w-full rounded-xl border border-white/10 bg-[#091322] px-3 outline-none"/></label><label className="text-sm text-slate-300">الوصف<textarea value={description} onChange={e=>setDescription(e.target.value)} maxLength={500} className="mt-2 min-h-24 w-full rounded-xl border border-white/10 bg-[#091322] p-3 outline-none"/></label><div className="flex gap-2"><Button type="submit">حفظ المشروع</Button><Button type="button" variant="ghost" onClick={()=>setOpen(false)}>إلغاء</Button></div></form></CardContent></Card>:<Button className="mb-5" onClick={()=>setOpen(true)}><Plus className="size-4"/>مشروع جديد</Button>}{projects.length?<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{projects.map(project=><Card key={project.id}><CardContent><div className="flex justify-between"><span className="grid size-11 place-items-center rounded-xl bg-cyan-300/8 text-cyan-300"><FolderKanban/></span><button aria-label={`حذف ${project.name}`} onClick={()=>remove(project.id)} className="grid size-11 place-items-center text-slate-600 hover:text-rose-300"><Trash2 className="size-4"/></button></div><h2 className="mt-4 font-bold text-white">{project.name}</h2><p className="mt-2 min-h-10 text-sm text-slate-500">{project.description||"بدون وصف"}</p><span className="mt-4 block text-xs text-slate-600">0 تسجيلات</span></CardContent></Card>)}</div>:<div className="rounded-2xl border border-dashed border-white/10 p-12 text-center"><FolderKanban className="mx-auto mb-3 size-9 text-slate-600"/><h2 className="font-bold text-white">لا توجد مشاريع بعد</h2><p className="mt-2 text-sm text-slate-500">اجمع تسجيلات الحملة أو الأسبوع في مشروع واحد.</p></div>}</>}
