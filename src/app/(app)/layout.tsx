import { redirect } from "next/navigation"; import { AppShell } from "@/components/layout/app-shell"; import { SetupRequired } from "@/components/layout/setup-required"; import { getAuthorizedUser } from "@/features/auth/require-user";
export default async function ProtectedLayout({children}:{children:React.ReactNode}){const result=await getAuthorizedUser();if(result.status==="unconfigured")return <SetupRequired/>;if(result.status!=="authorized"||!result.user)redirect(`/login?reason=${result.status}`);return <AppShell email={result.user.email??"الحساب الشخصي"}>{children}</AppShell>}
